<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ValleyClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ValleyClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ValleyClient">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="12,5,0,10">
            <TextBlock Text="valley" FontSize="20" FontWeight="SemiBold"/>
        </StackPanel>

        <NavigationView Grid.Row="1" x:Name="MainNavView">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="聊天" Icon="Message"/>
                <NavigationViewItem Content="好友列表" Icon="Message"/>
                <NavigationViewItem Content="组队房间" Icon="People"/>
            </NavigationView.MenuItems>

            <Frame x:Name="MainFrame"/>
        </NavigationView>
    </Grid>
</Window>
